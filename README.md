### Configuring Environment
1. Install Python and [miniconda](https://repo.anaconda.com/miniconda/)
2. Add channels: `conda config --add channels conda-forge`
3. Create environment: `conda env create -f environment.yml`
4. Activate environment: `conda activate frc-ai-scouter`
5. **Run the desired file!**